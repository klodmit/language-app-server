---
meta:
  environment: jvm
  toolchain:
    name: gradle
    version: 17
build:
  artifacts:
    build/libs/*.jar: /
run:
  jarName: ru.klodmit.language-app-0.0.1.jar
  persistenceMount: /data
  containerPort: 5000
